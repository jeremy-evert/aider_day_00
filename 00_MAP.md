# 00 — Day 0 Map

Work through these files in order.

Your build happens inside:

```text
jeremy_run_1/
```

The route is:

```text
FIND → CREATE → WRITE → RUN → BASELINE → PREDICT → AIDER → DIFF → PROOF → DECIDE
```

The durable engineering loop underneath it is:

```text
GOAL → BASELINE → AIDER → DIFF → PROOF → COMMIT
```

## Rules for this run

- One tiny behavior at a time.
- Do not repair classroom plumbing inside the exercise.
- Aider's success message is a claim, not proof.
- Inspect the diff.
- Run the program independently.
- You decide what survives.

When a step is green, move to the next numbered file.
