# Missions 010–011 — safe correction summary

Mission 010 found a caller/transport problem and a rejected partial deletion.
The worker was stopped, the production target restored, and no partial edit
was accepted merely because a tool emitted output.

Mission 011 repaired literal `Flo.ps1 --Terra-medium ...` parsing for dry-run
grammar. In the controlled Chain Gun fixture, Aider 0.86.2 with supported
`udiff` passed the Chamber 1 oracle and five regressions twice after fresh
resets. The earlier `diff` attempt hallucinated an unrelated path and exceeded
60 seconds. The successful replays took about 41.34 and 26.20 seconds, so the
transport was mechanically reliable but not classroom-fit for repeated live
bites.
