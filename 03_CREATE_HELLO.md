# 03 — Create the First Program

Inside `jeremy_run_1`, create:

```text
hello.py
```

Put exactly this in it:

```python
print("Hello, world!")
```

Save the file.

## Green light

`jeremy_run_1\hello.py` exists and contains one line.

Do not add functions, imports, logging, tests, or decorations. Tiny is the point.
