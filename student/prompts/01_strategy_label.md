# Bite 01 — name the strategy label

Add `SWITCH_LABEL = "switch"` directly below `DOORS` in
`monty_hall.py`. Change no other file and do not change runtime behavior.

Before accepting the edit, inspect `git diff`, run `python -m py_compile
monty_hall.py`, run `python -m unittest -v`, and run `git diff --check`.
Stop if any other file changes.
